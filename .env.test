# .env.test - Конфігурація для тестового середовища Jest/Prisma

# Облікові дані PostgreSQL
# ВАЖЛИВО: Перевірте, чи відповідають логін та пароль вашому Docker-налаштуванню
DB_USER=itssayzy
DB_PASSWORD=IvanVitkovskiy2006
DB_HOST=localhost
DB_PORT=5432

# URL для ТЕСТОВОЇ бази даних (використовує whitty_test)
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/whitty_test?schema=public"