# Database Schema
    
## Overview
The database uses **PostgreSQL**. The schema is managed **Prisma ORM**.
Below is the description of the tables and their relationships.

### ER Diagram
![image](/uploads/erd-diagram/Screenshot%202025-12-07%20at%2015.07.14.png)

```markdown
### Таблиця: `users`

Стовпці:
| Стовпець | Тип | Обмеження | Опис |
|----------|-----|-----------|------|
| user_id  | SERIAL | PRIMARY KEY | Ідентифікатор користувача |
| username | VARCHAR(255) | UNIQUE, NOT NULL | username користувача |
| email    | VARCHAR(255) | NULL | Email користувача |
| password | VARCHAR(255) | NOT NULL | пароль(я передаю вже хешований) |
|  avatar  | VARCHAR(255) | UNIQUE, DEFAULT(src to default.png) | урла до аватарки|
| country_id  | SERIAL | FK | айді країни |

Зв'язки:
Один-до-багатьох з posts (один юзер — багато постів).
Один-до-багатьох з comments (один юзер — багато коментарів).
Багато-до-одного з countries (юзер належить одній країні).
```

```markdown
### Таблиця: `posts`

Призначення: Зберігає інформацію про облікові записи користувачів

Стовпці:
| Стовпець | Тип | Обмеження | Опис |
|----------|-----|-----------|------|
| post_id  | SERIAL | PK | Ідентифікатор поста |
| user_id  | SERIAL | FK | Ідентифікатор користувача |
| caption  | VARCHAR(255) | NOT UNIQUE, DEFAULT NULL | загаловок |
| content  | VARCHAR(255) | NOT UNIQUE, DEFAULT NULL | інфа в пості(типу контент) |
| imageUrl | VARCHAR(255) | UNIQUE, DEFAULT NULL | урла до імеджу поста|
| likes_count | VARCHAR(255) | DEFAULT 0 | колво лайків на пості |
| comment_count | VARCHAR(255) | DEFAULT 0 | колво комментів на пості |


Зв'язки:
**Один-до-багатьох з posts (один юзер — багато постів).**
**Один-до-багатьох з comments (один юзер — багато коментарів).**
**Багато-до-одного з countries (юзер належить одній країні).**
```
```markdown
### Таблиця: `countries`

Стовпці:
| Стовпець | Тип | Обмеження | Опис |
|----------|-----|-----------|------|
| country_id  | SERIAL | PRIMARY KEY | Ідентифікатор країни |
| country_name | VARCHAR(255) | UNIQUE, NOT NULL | назва країни |

Зв'язки:
 **Один-до-багатьох з users.**


```markdown
### Таблиця: `comments`

Стовпці:
| Стовпець | Тип | Обмеження | Опис |
|----------|-----|-----------|------|
| comment_id  | SERIAL | PK | Ідентифікатор коментаря |
| post_id  | SERIAL | FK | Ідентифікатор поста де був цей ком |
| user_id  | SERIAL | FK | Ідентифікатор юзера який зробив ком |
| comment_text  | VARCHAR(255) | NOT UNIQUE, DEFAULT NULL | текст коментаря |
| createad_at  | TIMESTEMP | DEFAULT NULL | когда зроблен(час) |


Зв'язки:

```
```markdown
### Таблиця: `likes`

Призначення: Таблиця зв'язку для зберігання лайків. Гарантує, що юзер може лайкнути пост тільки один раз.

Стовпці:
| Стовпець | Тип | Обмеження | Опис |
|----------|-----|-----------|------|
| user_id  | INT |    FK     | Хто лайнув
| post_id  | INT |    FK     | На який пост був зробленний

Індекси:
   @@id([user_id, post_id]): Композитний первинний ключ — запобігає дублюванню лайків від одного й того ж користувача на одному пості.
```

```markdown
### Таблиця: `follower`

Стовпці:
| Стовпець | Тип | Обмеження | Опис |
|----------|-----|-----------|------|
| following_id  | SERIAL | PK | на кого підписався|
| follower_id  | SERIAL | PK | айди фоловера |
| follower_at | TIMESTEMP | DEFAULT NULL | час коли був зафоловений|

 Індекси:

    @@id([follower_id, following_id]): Композитний первинний ключ.

```

## 🛠 Рішення щодо дизайну 

У цьому розділі пояснюється логіка вибору архітектури бази даних, компроміси між продуктивністю та цілісністю даних, а також стратегія індексації.

### 1. Структура схеми
Було обрано класичну **реляційну модель**, оскільки дані мають чітку структуру та сильні зв'язки (користувачі, пости, коментарі).

* **Центрична модель користувача:** Таблиця `users` є центральною сутністю, до якої прив'язані майже всі інші таблиці через зовнішні ключі (`Foreign Keys`).
* **Таблиці зв'язків (Junction Tables):** Для реалізації зв'язків "Багато-до-Багатьох" (N:M) у лайках та підписках використано окремі таблиці (`likes`, `followers`). Це дозволяє ефективно керувати зв'язками без використання масивів або JSON-структур, що покращує цілісність даних.
* **Довідник країн:** Країни винесені в окрему таблицю `countries` (нормалізація), щоб уникнути дублювання текстових назв у таблиці користувачів та забезпечити стандартизацію даних.

### 2. Рівень нормалізації
Схема бази даних здебільшого відповідає **Третій нормальній формі (3NF)**:
* Усі атрибути є атомарними (1NF).
* Усі неключові атрибути залежать від первинного ключа (2NF).
* Відсутні транзитивні залежності між неключовими атрибутами (3NF).

**Виняток (Денормалізація):**
У таблиці `posts` ми свідомо порушили нормалізацію, додавши поля:
* `likes_count`
* `comments_count`

**Причина:** Ці поля дублюють інформацію, яку можна отримати через `COUNT(*)`. Однак для стрічки новин (Feed) обчислення кількості лайків "на льоту" для кожного поста створювало б величезне навантаження на базу даних (N+1 проблема).

### 3. Зроблені компроміси 

#### Продуктивність читання vs. Складність запису

* **Рішення:** Використання лічильників (`likes_count`) у таблиці `posts`.


* **Плюс:** Миттєве завантаження стрічки новин без складних агрегацій.


* **Мінус:** Необхідність підтримувати актуальність лічильників при кожному додаванні/видаленні лайка (транзакційність). Є ризик розсинхронізації даних при збоях, що потребує періодичних скриптів для перерахунку ("Repair scripts").

####  Зберігання медіа
* **Рішення:** Зберігання шляхів до файлів (String) у БД, а самих файлів — на диску сервера.

* **Плюс:** База даних залишається "легкою" та швидкою, бекапи займають менше місця.

* **Мінус:** Втрачається транзакційна цілісність між файловою системою та БД (якщо видалити рядок у БД, файл на диску залишається, і навпаки).


### 4. Стратегія індексування

Для забезпечення високої швидкості запитів використано наступні індекси:
  **Унікальні індекси:**
    * `users(username)` та `users(email)`: Для швидкої перевірки унікальності при реєстрації та миттєвого пошуку при логіні.

    
  **Композитні (складені) індекси:**
    * **Таблиця `likes` (`user_id`, `post_id`):** Цей індекс виконує подвійну роль. По-перше, він є первинним ключем. По-друге, він дозволяє миттєво перевірити, чи лайкнув конкретний юзер конкретний пост (запит вигляду `WHERE user_id = X AND post_id = Y`).


    * **Таблиця `followers` (`follower_id`, `following_id`):** Аналогічно, забезпечує унікальність підписки та швидкий пошук зв'язків.
